services:
  go2rtc:
    image: alexxit/go2rtc:latest
    container_name: evosecure-go2rtc
    restart: unless-stopped
    ports:
      - "1984:1984" # WebUI + API
      - "8554:8554" # RTSP server (optional)
    volumes:
      - ./go2rtc.yaml:/config/go2rtc.yaml:ro
